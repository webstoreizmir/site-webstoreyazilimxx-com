---
interface Feature {
  title: string;
  desc: string;
}

interface Props {
  features: Feature[];
}

const { features } = Astro.props;

// Icons for each feature
const icons = [
  `<svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>`,
  `<svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>`,
  `<svg class="w-10 h-10" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>`,
];
---

<section id="features" class="py-24 relative">
  <div class="container mx-auto px-6">
    <h2 class="text-4xl md:text-5xl font-heading font-bold text-center mb-16 bg-clip-text text-transparent bg-gradient-to-r from-primary to-secondary">
      Neden Bizi Seçmelisiniz?
    </h2>
    
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {features.map((feature, index) => (
        <div class="glass rounded-2xl p-8 hover:scale-105 hover:shadow-2xl transition-all duration-300 group cursor-pointer">
          <!-- Icon -->
          <div class="w-16 h-16 rounded-xl bg-gradient-to-br from-primary to-secondary flex items-center justify-center mb-6 text-white group-hover:rotate-12 transition-transform duration-300">
            <Fragment set:html={icons[index % icons.length]} />
          </div>
          
          <!-- Content -->
          <h3 class="text-2xl font-heading font-bold mb-3 text-text group-hover:text-primary transition-colors">
            {feature.title}
          </h3>
          <p class="text-text/70 font-body leading-relaxed">
            {feature.desc}
          </p>
          
          <!-- Hover Indicator -->
          <div class="mt-6 flex items-center text-primary opacity-0 group-hover:opacity-100 transition-opacity">
            <span class="text-sm font-medium">Daha fazla</span>
            <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
            </svg>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
